import {menuItems} from "@/lib/menu"; export default async function Search({searchParams}:{searchParams:Promise<{q?:string}>}){const q=((await searchParams).q||"").trim().toLowerCase();const results=q?menuItems.filter(x=>(x.name+" "+x.category+" "+x.description).toLowerCase().includes(q)):[];return <main className="container section"><div className="eyebrow">Search</div><h1>{q?"Results for “"+q+"”":"Search Cook Out"}</h1>{!q&&<p className="muted">Enter a menu item, category or topic in the homepage search box.</p>}<div className="grid">{results.map(x=><a className="card" href={"/menu/"+x.slug} key={x.slug}><h2>{x.name}</h2><p className="muted">{x.category}</p></a>)}</div>{q&&!results.length&&<p className="muted">No matching source-checked item found yet.</p>}</main>}