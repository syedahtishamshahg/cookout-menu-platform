import {menuItems} from "@/lib/menu"; import {notFound} from "next/navigation"; import type {Metadata} from "next";
export function generateStaticParams(){return menuItems.map(x=>({slug:x.slug}));}
export async function generateMetadata({params}:{params:Promise<{slug:string}>}):Promise<Metadata>{const {slug}=await params;const item=menuItems.find(x=>x.slug===slug);return item?{title:item.name,description:item.description}:{title:"Menu Item"};}
export default async function ItemPage({params}:{params:Promise<{slug:string}>}){const {slug}=await params;const item=menuItems.find(x=>x.slug===slug);if(!item)notFound();return <main className="container section"><div className="eyebrow">{item.category}</div><h1>{item.name}</h1><p className="muted">{item.description}</p><div className="grid" style={{marginTop:24}}><div className="card"><h2>Nutrition</h2><p><strong>{item.nutrition?.calories}</strong> calories</p><p>{item.nutrition?.protein_g}g protein</p><p>{item.nutrition?.sodium_mg}mg sodium</p></div><div className="card"><h2>Source status</h2><p>Source-checked reference data.</p><p className="muted">Prices are location-dependent and will be shown only when sourced and verified.</p></div></div><p style={{marginTop:28}}><a href="/menu/">← Back to menu</a></p></main>}