import {locations} from "@/lib/locations";
export const metadata={title:"Cook Out Locations",description:"Cook Out location information with source and verification status."};
export default function Locations(){return <main className="container section"><div className="eyebrow">Location Finder</div><h1>Cook Out Locations</h1><p className="muted">We are building a source-backed location directory. No address is published until it can be tied to a reliable source.</p><div className="card" style={{marginTop:24}}><h2>{locations.length?"Locations available":"Location database ready"}</h2><p className="muted">{locations.length?"Currently showing "+locations.length+" location records.":"The database structure is ready for verified location records, hours and local pricing."}</p></div></main>}