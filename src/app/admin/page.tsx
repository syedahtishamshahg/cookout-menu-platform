import {reviewQueue} from "@/lib/admin";
export const metadata={title:"Admin Dashboard"};
export default function Admin(){const pending=reviewQueue.filter(x=>x.status==="pending").length;return <main className="container section"><div className="eyebrow">Restricted Area</div><h1>Admin Dashboard</h1><p className="muted">Administration UI foundation. Authentication and server-side authorization are required before production use.</p><div className="grid" style={{marginTop:24}}><div className="card"><h2>Pending Reviews</h2><p style={{fontSize:32,fontWeight:800}}>{pending}</p></div><div className="card"><h2>Verification Queue</h2><p className="muted">Prices, locations, nutrition and corrections will flow through review.</p></div><div className="card"><h2>Audit Log</h2><p className="muted">Record who acted, when, what changed and why.</p></div></div></main>}