"use client"; import {useState} from "react"; import {submissionSchema} from "@/lib/submissions";
export default function Contact(){const [sent,setSent]=useState(false);const [message,setMessage]=useState("");return <main className="container section"><div className="eyebrow">Data Quality</div><h1>Contact & Corrections</h1><p className="muted">Send a correction or source suggestion. Submissions are reviewed before publication.</p><form className="card" style={{marginTop:24,maxWidth:720}} onSubmit={e=>{e.preventDefault();const f=new FormData(e.currentTarget);const r=submissionSchema.safeParse({type:f.get("type"),page:f.get("page"),message:f.get("message"),source:f.get("source")||undefined});if(!r.success){setMessage("Please complete the required fields.");return}setSent(true);setMessage("Submission recorded locally for this development build. Production storage will use the review queue.");}}><label>Type<select name="type" style={{display:"block",width:"100%",padding:12,marginTop:6}}><option value="correction">Correction</option><option value="price">Price update</option><option value="location">Location update</option><option value="nutrition">Nutrition update</option></select></label><label style={{display:"block",marginTop:16}}>Page or item<input name="page" required style={{display:"block",width:"100%",padding:12,marginTop:6}}/></label><label style={{display:"block",marginTop:16}}>Details<textarea name="message" required minLength={10} rows={6} style={{display:"block",width:"100%",padding:12,marginTop:6}}/></label><label style={{display:"block",marginTop:16}}>Supporting source URL<input name="source" type="url" style={{display:"block",width:"100%",padding:12,marginTop:6}}/></label><button className="btn" style={{marginTop:18}} disabled={sent}>Submit</button>{message&&<p className="muted" role="status">{message}</p>}</form></main>}