"use client";
import {useState} from "react"; import {shakeFlavors} from "@/lib/tools";
export default function ShakeMixer(){const [a,setA]=useState("");const [b,setB]=useState("");const mix=a&&b?a+" + "+b:a||b||"Choose two flavors";return <main className="container section"><div className="eyebrow">Interactive Tool</div><h1>Cook Out Shake Mixer</h1><p className="muted">Create a two-flavor combination. This tool describes a combination; it does not claim that every combination is an official menu item.</p><div className="grid" style={{marginTop:24}}><div className="card"><h2>Flavor 1</h2><select value={a} onChange={e=>setA(e.target.value)} style={{width:"100%",padding:12}}><option value="">Choose flavor</option>{shakeFlavors.map(x=><option key={x}>{x}</option>)}</select><h2 style={{marginTop:20}}>Flavor 2</h2><select value={b} onChange={e=>setB(e.target.value)} style={{width:"100%",padding:12}}><option value="">Choose flavor</option>{shakeFlavors.map(x=><option key={x}>{x}</option>)}</select></div><div className="card"><h2>Your Mix</h2><p style={{fontSize:24,fontWeight:800}}>{mix}</p><p className="muted">Availability, price and nutrition should be confirmed locally.</p><button className="btn" onClick={()=>{setA("");setB("")}}>Reset</button></div></div></main>}