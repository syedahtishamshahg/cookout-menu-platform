# Next Action

The codebase is prepared for the Cloudflare configuration step.

Required external action:
1. Create the real D1 database.
2. Obtain its database ID.
3. Configure the D1 binding.
4. Run migrations.
5. Configure a real authentication provider/session mechanism.
6. Configure Turnstile/rate limiting.

Until those are configured, the repository must remain in development/pre-launch status.
